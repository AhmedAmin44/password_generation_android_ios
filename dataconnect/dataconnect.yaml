specVersion: "v1beta"
serviceId: "passwordgen"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "passwordgen-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
